[project]
name = "db-helper"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "psycopg[binary]>=3.3.4",
    "pymongo>=4.17.0",
    "pytest>=9.1.1",
    "ruff>=0.15.20",
    "tomlkit>=0.15.0",
]

[tool.uv]
package = false
index-url = "https://artifact.stengglink.com/repository/pypi-proxy/simple"
